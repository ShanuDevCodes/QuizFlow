<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.QuizFlow" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Shown while installSplashScreen() prepares the first frame (F1: "brief splash or
         loading indicator"). Falls back to Theme.QuizFlow once content is ready. -->
    <style name="Theme.QuizFlow.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.QuizFlow</item>
    </style>
</resources>